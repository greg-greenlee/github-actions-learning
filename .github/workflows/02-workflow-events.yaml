name: Workflow Events
on: 
  workflow_dispatch:
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "This here workflow was triggered by a ${{ github.event_name }} event"
